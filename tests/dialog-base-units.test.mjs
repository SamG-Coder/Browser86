import test from 'node:test';import assert from 'node:assert/strict';import {guest} from './helpers.mjs';
test('Dialog base units agree with the virtual system-font alphabet extent',()=>{const {p}=guest('HelloConsole.exe'),m=p.memory,u=(n,...a)=>p.apis.lookup('user32.dll',n).fn(...a),g=(n,...a)=>p.apis.lookup('gdi32.dll',n).fn(...a),dc=p.apis.gui.newDC(0),out=p.heap.alloc(8),alphabet=p.heap.string('ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz');g('SelectObject',dc,g('GetStockObject',13));g('GetTextExtentPoint32A',dc,alphabet,52,out);const expectedWidth=Math.floor((Math.floor(m.u32(out)/26)+1)/2),height=m.u32(out+4);p.lastError=123;const units=u('GetDialogBaseUnits');assert.equal(units&65535,expectedWidth);assert.equal(units>>>16,height);assert.equal(p.lastError,123);assert.equal(u('GetDialogBaseUnits'),units);});
