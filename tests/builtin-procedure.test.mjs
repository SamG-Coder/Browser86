import test from 'node:test';import assert from 'node:assert/strict';import {guest} from './helpers.mjs';
test('Subclassing a built-in EDIT returns a callable original procedure and supports restoring it',()=>{const {p}=guest('HelloConsole.exe'),gui=p.apis.gui,u=(n,...a)=>p.apis.lookup('user32.dll',n).fn(...a),w={hwnd:1,builtin:true,className:'EDIT',proc:0,wide:false,style:0,title:'',width:120,height:30,enabled:true};gui.windows.set(1,w);const original=u('GetWindowLongA',1,-4);assert.notEqual(original,0);assert.equal(u('SetWindowLongA',1,-4,0x401000),original);const call=u('CallWindowProcA',original,1,0xcf,1,0);assert.equal(call.call.address,original);const entry=[...p.apis.traps.values()].find(e=>e.name==='#Builtin.EDITA');assert.ok(entry);assert.equal(entry.fn(1,0xcf,1,0),1);assert.equal(w.style&0x800,0x800);assert.equal(w.proc,0x401000);assert.equal(u('SetWindowLongA',1,-4,original),0x401000);assert.equal(w.proc,0);assert.equal(u('GetWindowLongA',1,-4),original);});
